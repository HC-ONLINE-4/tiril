# TikTok Live Recorder - Dependencias (escritorio + servidor)
TikTokLive>=6.0.0
httpx>=0.24.0
playwright>=1.40.0
plyer>=2.1.0

# Servidor headless + web privada
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9

# GitHub Actions + Google Drive
google-api-python-client>=2.120.0
google-auth>=2.27.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Nota: ffplay (incluido con ffmpeg) se usa para reproducir video
# Asegurate de que ffmpeg este instalado y en el PATH
